# Copie este arquivo para .env e preencha com seus valores reais
# .env e ignorado pelo git e nao deve ser commitado

# Banco de dados MySQL (com fallback automatico para SQLite local se inacessivel)
DB_HOST=localhost
DB_PORT=3306
DB_USER=seu_usuario
DB_PASSWORD=sua_senha_aqui
DB_NAME=ortomapas

# URLs de servicos Docker
WEBODM_URL=http://localhost:8000
GEOSERVER_URL=http://localhost:8080/geoserver

# API FastAPI
API_HOST=0.0.0.0
API_PORT=8888

# Opcional — chave da API Anthropic para geracao de relatorios (ReportAgent)
# Sem isso, o ReportAgent usa um template simples sem LLM
# Para usar, descomente a linha abaixo e adicione sua chave real:
# ANTHROPIC_API_KEY=<your-anthropic-api-key-here>

# Grafana (obrigatorio ao subir a stack de observabilidade)
GRAFANA_ADMIN_PASSWORD=gere-uma-senha-forte-aqui
